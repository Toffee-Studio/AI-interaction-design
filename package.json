{
  "name": "toffee-docs",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "npm run dev --prefix apps/docs",
    "build": "npm run build --prefix apps/docs",
    "start": "npm run start --prefix apps/docs"
  },
  "engines": {
    "node": ">=18"
  }
}
